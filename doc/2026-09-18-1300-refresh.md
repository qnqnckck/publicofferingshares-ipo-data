# 2026-09-18 13:00 KST scheduled refresh

## Idea summary

Automation 13. Clock verified 2026-09-18 04:01:03 UTC / 13:01:03 Asia/Seoul. Reconcile the full September/October 2026 window `[2026-09-01, 2026-11-01)` against the manual publication completed earlier today (main `1207bf5013ebcb47ae1cfb09267ac368f0c9a446`).

## MVP scope

- Problem: live subscription figures become stale between checked publications.
- Must-have: fresh calendar/filing/news comparison across all 24 current/next-month events; apply only new source-confirmed facts; rebuild and validate public feeds; scoped main publication if gates pass.
- Out of scope: November listings, app/store releases, Actions, invented estimates, missing-as-zero input, unrelated history or user changes.
- Success: new timestamped source values reach stock and summary feeds without changing unrelated facts. Every monthly audit ERROR blocks commit/push.

## Feature specification

1. Identity/calendar: recheck 38, StayRich, Pikumo, Sonnimlab and current filing timeline; compare exact name/window with the same-day ledger. Exclude rights offerings and out-of-window candidates. Retain primary-confirmed dates over stale secondary dates.
2. Fundamentals: compare newly published evidence to the 11:00 primary DART/issuer review; do not overwrite verified fields with stale band prices or application-basis lockup.
3. Retail: persist Brils source timestamp, actual pool/count and published competition rates. Do not input a calculated equal expectation or subscribed-share estimate; these remain null. Existing app/report calculations may derive expectations from actual pool/count, but they are not supplied as new observed facts. Post-close Global/Duksan source observations are unchanged.
4. Validation/publication: isolated local-only rebuild, one-stock/coherent-feed application, JSON parsing, full monthly audit, Finuts schedule validator, Dart analysis, diff scope check, then authorized main push and exact production URL verification. Failures are documented; no audit bypass.

## Wireframe

```text
Fresh full-month candidate check -> source-confirmed delta
 -> timestamped canonical broker snapshot -> isolated batch
 -> scoped stock/feed merge -> audit + JSON + schedule + Dart
 -> gates pass: main push + served-feed checks
```

## Evidence observed

- [38 calendar](https://www.38.co.kr/html/fund/index.htm?o=k), [StayRich](https://stayrich.life/ipo-calendar), [Pikumo](https://pikumo.net/ipo), [Sonnimlab](https://www.sonnimlab.com/schedule/), [filing timeline](https://stock.startuprecipe.co.kr/): all 24 in-window candidates remain covered. 38 now lists Hanwha/iM SPACs already added earlier today; alias changes do not create new events. Baropharm is Nov 2-3, outside the two-month scope. Ucast remains supported by the primary filing linked in the existing ledger despite omission on 38. New schedule/news search did not identify a verified in-window delta to the earlier same-day DART review. Some secondary sites retain stale dates; they do not override primary filings.
- [Npay Brils operands](https://m.stock.naver.com/front-api/ipo/calculator/operands?code=A468670): source `2026-09-18T12:59:43+09:00`; IBK applications **75,440**, equal pool **150,000**, proportional pool **150,000**, total competition **860.86**, proportional **1,721.71**. Intraday, not final. Do not double the rounded total (would incorrectly yield 1,721.72).
- [Duksan operands](https://m.stock.naver.com/front-api/ipo/calculator/operands?code=A266690) still Sep 17 16:01:19, and [Global operands](https://m.stock.naver.com/front-api/ipo/calculator/operands?code=A486510) still Sep 17 16:01:17; all relevant values match the previous publication.

## Validation/publication

- Scope: September/October 2026, 24 candidates matched this run, 0 added, 0 excluded in-window, 0 unresolved. Same-day ledger retains the morning baseline statuses (19 matched / 5 added).
- Monthly audit: **0 ERROR, 17 WARNING**. Pending warnings remain 15 October listing dates and two SPAC application counts. No values were guessed.
- Local-only generation PASS (481 records in isolated output); only Brils detail plus index/dashboard/active/recent/yearly-2026 were applied. **484 unrelated index rows preserved byte-for-byte**. No app/scoring code was changed.
- JSON parsing PASS (10 changed/new JSON documents), latest-only Brils snapshot assertions PASS, unpublished expectedEqualShares/subscribedShares remain null. Regression executable PASS; Dart batch static analysis PASS (`No issues found`). Diff whitespace check PASS.
- Finuts schedule validator exit 0: local-seed fallback PASS. Live request timed out (WinError 10060), so live Finuts parity is not claimed; independent fresh calendars/filings remain the schedule evidence.
- The existing report calculation derives 1.9883 equal shares from the actual 150,000 pool / 75,440 applications; this is a labelled model output, not a manually supplied allocation fact or guarantee. The generated allocation/profit model now uses the same latest observation.
- Generator-only discovery/identifier normalization was restored. Pre-existing identifier worktree status and earlier untracked audit notes are preserved. Generation time is never substituted for observation time.

Commands (nested repository; Dart `D:\harness\flutter\bin\dart.bat`, bundled Python):

```text
dart run tool/ipo_competition_batch.dart --backfill-years 3 --manual-fundamentals-path data/manual_fundamentals.json --no-discover --no-public-live-collect --no-identifier-discover --no-ipo-korea-supplement --no-article-lead-manager-discover --out build/refresh-20260918-1300/output
python -X utf8 build/apply_reviewed_20260918_1300.py
audit_monthly_ipo_data.ps1 -RepoPath D:\harness\projects\publicofferingshares\ipo-data -CandidateLedgerPath doc/monthly-ipo-audit-2026-09-18.json -AsOfDate 2026-09-18
dart analyze tool/ipo_competition_batch.dart
dart run tool/ipo_pending_counts_test.dart
python -X utf8 tool/validate_finuts_schedule_sync.py --warn-on-analysis-issues
Get-Content <each changed/new JSON> -Raw -Encoding UTF8 | ConvertFrom-Json
git -c core.safecrlf=false diff --check
```

Published data commit `f675f946c3da5ddce8ec23c4837f31d7410a3df2` to `main` at `https://github.com/qnqnckck/publicofferingshares-ipo-data.git`. Push succeeded and `git ls-remote origin refs/heads/main` matched local HEAD. Direct production Raw requests (no cache-busting query) for Brils stock detail and active/index/dashboard/recent/yearly-2026 all matched the local JSON structurally: **6/6 PASS**. No Actions or store release was involved. Pending warnings and the Finuts fallback remain explicitly documented above.

Freshness correction discovered during generated-output review: the app can backfill an older explicit equal expectation into a newer null expectation, and the batch's expected-allocation helper takes the maximum across historical snapshots. To avoid mixing capture times without changing app/scoring code, superseded Brils 11:29 input is preserved verbatim in `doc/2026-09-18-1129-brils-observation.json` and removed from the active broker input file. Its old pools/counts and provenance remain recoverable in that archive and Git history. The new canonical row contains only directly published values and nullable unpublished fields.
