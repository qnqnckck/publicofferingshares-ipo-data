# 2026-09-18 17:00 KST retail-only refresh

## Idea summary

Automation 13; clock verified 2026-09-18 08:01:40 UTC / 17:01:40 KST. Only today's subscription window is eligible: Brils (`브릴스_2026-09-17`, September 17-18, IBK). Starting main: `4db079f1b25692cd7add0a5124cef7297fc1fb01`.

## MVP scope

- Problem: today's broker subscription count and proportional competition changed since 12:59.
- Must-have: source-confirmed current broker metrics, truthful capture time/finality, coherent regenerated stock/feed views, required data checks, scoped publication.
- Out of scope: schedule, demand forecast, fundamentals, listing results, other companies, app/UI/code, Actions and store releases.
- Success: latest verified retail observation is published without changing frozen fields or inventing values; every audit ERROR blocks commit/push.

## Feature specification

1. Exact scope: select existing events with start <= September 18 <= end; one candidate matches. Reuse the same-day 24-event September/October audit ledger without reclassifying or changing other events. The 17:00 user scope overrides full-month discovery from the general skill.
2. Actual facts only: Npay calculator source time September 18 16:06:02 KST, IBK applications 113,800, equal/proportional pools 150,000 each, proportional competition 3,352.52. Published total 1,676.26 travels with the same retail snapshot so total and proportional fields do not represent different observation times. Offer/deposit/allocation baselines are carried unchanged. Unknown exact subscribed shares and explicit expected equal shares remain null; no estimate is manually entered.
3. Preserve freshness: archive the superseded 12:59 observation outside active batch inputs, retaining exact evidence and Git history. The existing app/model computes a labelled equal expectation from the current actual pool/count; it is not guaranteed allocation. Mark the source as post-close observation, not final allotment.
4. Verification: local-only isolated generation, one-stock/five-feed merge, freeze-field and unrelated-row assertions, JSON/monthly audit/schedule-sync/diff checks. No Dart source change or 13:00 full refresh in this run. Push main only after all required checks pass and verify stock plus applicable feed URLs.

## Wireframe

```text
Today's exact subscription windows -> verified broker observation
 -> archive superseded row -> canonical latest row -> local-only batch
 -> scope/frozen fields + JSON + monthly audit + schedule validation
 -> main push -> exact production stock/feed verification
```

## Source evidence

- [Npay calculator](https://m.stock.naver.com/front-api/ipo/calculator/operands?code=A468670): fetched after 17:00; source baseTime/updtTm both 2026-09-18T16:06:02. This is not a 17:00 source timestamp. Only published positive broker values are used.
- [Npay issuer page](https://ustock.naver.com/service/ipo/468670), [independent event page](https://onedaytrading.net/ipo/%EB%B8%8C%EB%A6%B4%EC%8A%A4) and [weekly calendar](https://moajoo.com/ipo-20260913/) corroborate September 17-18 / IBK identity. Cached issuer-page retail values are September 17 and stale; they do not override the fresh calculator response. No schedule/fundamental values from these pages are applied.
- The website search did not establish a final broker allotment announcement. Preserve the cautious `post_close_observation` source label.

## Validation/publication

- Today's scope: one candidate matched, zero added/excluded/unresolved. The September/October audit covers all 24 events; the existing same-day ledger remains unchanged.
- Monthly audit: **0 ERROR, 17 WARNING**. Warnings remain 15 unconfirmed October listing dates and two SPAC application counts. Unpublished values were not invented.
- Isolated generation PASS (481 stock files). Scoped merge PASS: one Brils stock detail and five feeds (index/dashboard/active/recent/yearly-2026); **484 unrelated index rows preserved byte-for-byte**.
- Frozen-field assertions PASS: every top-level Brils field outside snapshots/analysis is unchanged. Exactly one current snapshot remains. Canonical expectedEqualShares and subscribedShares stay null. The existing model derives 1.3181 equal shares from 150,000 / 113,800, not a guaranteed allocation or a manually supplied fact.
- JSON parsing PASS (nine changed/new JSON documents); pending-count regression executable PASS; whitespace diff check PASS. No Dart source was changed; the 13:00-only static-analysis gate does not apply to this 17:00 run.
- Schedule validator exit 0: **local-seed fallback PASS**. The live Finuts request timed out (WinError 10060), so live Finuts parity is not claimed.
- Generator-only discovery/identifier normalization was restored. Existing unrelated identifier status and older untracked reports are preserved. No app/UI/scoring code, schedule, fundamentals, automation settings, Actions or store release was changed.
- The superseded 12:59 active input was moved out of batch inputs; its complete observation is recoverable in `doc/2026-09-18-1259-brils-observation.json` and Git history. This prevents the existing historical-maximum/backfill behavior from mixing old and new allocation expectations.

Commands (nested repository; Dart `D:\harness\flutter\bin\dart.bat`, bundled Python):

```text
dart run tool/ipo_competition_batch.dart --backfill-years 3 --manual-fundamentals-path data/manual_fundamentals.json --no-discover --no-public-live-collect --no-identifier-discover --no-ipo-korea-supplement --no-article-lead-manager-discover --out build/refresh-20260918-1700/output
python -X utf8 build/apply_reviewed_20260918_1700.py
audit_monthly_ipo_data.ps1 -RepoPath D:\harness\projects\publicofferingshares\ipo-data -CandidateLedgerPath doc/monthly-ipo-audit-2026-09-18.json -AsOfDate 2026-09-18
dart run tool/ipo_pending_counts_test.dart
python -X utf8 tool/validate_finuts_schedule_sync.py --warn-on-analysis-issues
Get-Content <each changed/new JSON> -Raw -Encoding UTF8 | ConvertFrom-Json
git diff --exit-code -- data/discovered/ipo_events.json data/manual_fundamentals.json
git -c core.safecrlf=false diff --check
```

Publication receipt pending; commit/push only the reviewed paths after these gates.
